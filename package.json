{
  "name": "ppt",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel ./src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "gh-pages -d ./dist",
    "deploy-and-deploy": "npm run build && npm run deploy"
  }
}

