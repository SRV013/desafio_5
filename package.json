{
  "name": "ppt",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.7.0",
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /juego-ppt",
    "deploy": "npx gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  }
}